# UMLForge Frontend Environment

# Backend API URL — point this at your running backend.
# When set, all API calls go to the backend.
# When missing, the app runs in local-only mode (localStorage).
VITE_API_URL=http://localhost:8000
